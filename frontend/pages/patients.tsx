import Layout from "../components/Layout";

export default function Patients() {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
}

/**
 * diagnosis
 * med's list
 */

/**
 * in video see:
 * meds list + suggestion (pharm)
 * meds list (pat)
 */
